import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function Merenda() {
  return (
    <View style={{backgroundColor: 'fff', height: 1001}}>
      <Text style={{fontSize: 25, fontWeight: 'bold'}} >O Horario da Merenda só será liberado após o lançamento do App</Text>
    </View>
  );
}